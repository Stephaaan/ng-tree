import { IsNodeSelectedPipe } from './is-node-selected.pipe';

describe('IsNodeSelectedPipe', () => {
  it('create an instance', () => {
    const pipe = new IsNodeSelectedPipe();
    expect(pipe).toBeTruthy();
  });
});
